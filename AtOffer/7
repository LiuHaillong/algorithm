class Solution {
public:
    int Fibonacci(int n) {
        if(n<=0)
            return 0;
        int f = 0, g = 1;
        while(n--){
            g = g + f;
            f = g - f;
        }
        return f;
    }
};

/**
 * 动态规划版
 * 后面还要多理会理会
 */
